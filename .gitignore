node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.log
.DS_Store
next-env.d.ts
.vercel

# Cloudflare
.open-next/
.wrangler/

# ไฟล์ที่มีคีย์/ความลับ ห้าม commit เด็ดขาด
token.txt
*token*.txt

# ไฟล์ cache ของ TypeScript
tsconfig.tsbuildinfo

# การตั้งค่า Claude Code เฉพาะเครื่องนี้ (มีสิทธิ์ deploy อยู่ข้างใน)
.claude/settings.local.json

# ไฟล์ข้อมูลพนักงาน/ลูกค้า (PII) — repo นี้เป็น public ห้าม commit
*.xlsx
*.xls
*.csv
