-- ============================================================
-- เพิ่มอีเมลพนักงาน (ใช้ติดต่อ/ส่งรายงาน ไม่ได้ใช้เข้าระบบ — เข้าระบบใช้เบอร์มือถือ)
-- ไม่บังคับ unique เพราะบางคนอาจใช้อีเมลร่วมกัน และบางคนไม่มีอีเมล
-- ============================================================

alter table public.employees add column if not exists email text;

comment on column public.employees.email is 'อีเมลพนักงาน (ไม่บังคับ, ไม่ได้ใช้เข้าระบบ)';
